services:
  - type: web
    name: invoice-analytics-api
    env: node
    buildCommand: npm install && npx prisma generate
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: VANNA_API_BASE_URL
        sync: false
      - key: PORT
        value: 3001
      - key: NODE_ENV
        value: production
